local PersistenceConfig = {
	persistenceDataStoreName = "Cab87PlayerProfile",
	persistenceUseDataStore = true,
	-- Studio API access is commonly disabled for local Play Solo sessions.
	persistenceUseDataStoreInStudio = false,
}

return PersistenceConfig
